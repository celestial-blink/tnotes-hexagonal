const config = {
    PATH_BASE_API: "http://127.0.0.1:1112/api/",
    NAME_ACCESS_TOKEN: "accessToken",
    NAME_REFRESH_TOKEN: "refreshToken",
    KEY_DARK_MODE: "isDarkMode"
};

export default config;
